const submit = false;
const file = submit ? '/dev/stdin' : 'in';
const input = require('fs').readFileSync(file, 'utf8');
const lines = input.split('\n');

const x = (lines[0]);
const y = (lines [1]);

if(x%2==0){
    return x=impar;
}

//não consegui.